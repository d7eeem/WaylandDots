# This file sources other files in `hyprland` and `custom` folders
# You wanna add your stuff in file in `custom`

# ════════════════════════════════════════════════════════════════════
# UWSM MODE (Universal Wayland Session Manager)
# To use UWSM, ensure you start Hyprland with: uwsm start hyprland.desktop
# ════════════════════════════════════════════════════════════════════

# Core configuration
source = ./hypr.conf
source = ./hyprexec.conf
source = ./hyprkeys.conf
source = ./hyprenv.conf
source = ./rules.conf
source = ./monitors.conf
source = ./permission.conf
source = ./themes/hyprland-colors.conf
source = ./themes/common.conf
