[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="$HOME/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="$HOME/.config/fish/config.fish" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="$HOME/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="$HOME/.config/fish/config.fish" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "D" ] && export D="/home/tinker/Downloads" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "hs" ] && export hs="/home/tinker/.local/bin/hyde" 
[ -n "src" ] && export src="/home/tinker/.local/src" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "D" ] && export D="/home/tinker/Downloads" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "hs" ] && export hs="/home/tinker/.local/bin/hyde" 
[ -n "src" ] && export src="/home/tinker/.local/src" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "d" ] && export d="/home/tinker/Downloads/" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "d" ] && export d="/home/tinker/Downloads/" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "D" ] && export D="/home/tinker/Downloads" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
[ -n "cac" ] && export cac="/home/tinker/.cache" 
[ -n "cf" ] && export cf="/home/tinker/.config" 
[ -n "Cff" ] && export Cff="/home/tinker/.config/fish" 
[ -n "cfz" ] && export cfz="/home/tinker/.config/yazi" 
[ -n "cy" ] && export cy="/home/tinker/.config/yazi" 
[ -n "d" ] && export d="/home/tinker/Documents" 
[ -n "D" ] && export D="/home/tinker/Downloads" 
[ -n "do" ] && export do="/home/tinker/WaylandDots/" 
[ -n "dt" ] && export dt="/home/tinker/.local/share" 
[ -n "h" ] && export h="/home/tinker/" 
[ -n "hh" ] && export hh="/home/tinker/.config/hypr/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "nc" ] && export nc="/home/tinker/Nextcloud/" 
[ -n "pp" ] && export pp="/home/tinker/Pictures" 
[ -n "rr" ] && export rr="/home/tinker/.config/rofi/" 
[ -n "sc" ] && export sc="/home/tinker/.local/bin" 
[ -n "sd" ] && export sd="/home/tinker/.local/bin/dmenu" 
[ -n "ss" ] && export ss="/home/tinker/.local/bin/statusbar" 
[ -n "sp" ] && export sp="/home/tinker/.local/bin/personal" 
[ -n "vn" ] && export vn="/home/tinker/.config/nvim" 
[ -n "vnc" ] && export vnc="/home/tinker/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="/home/tinker/.config/nvim/lua/plugins" 
[ -n "wb" ] && export wb="/home/tinker/.config/waybar/" 
[ -n "bf" ] && export bf="/home/tinker/.config/shell/bm-files" 
[ -n "bd" ] && export bd="/home/tinker/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="/home/tinker/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="/home/tinker/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="/home/tinker/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="/home/tinker/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="/home/tinker/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="/home/tinker/.config/hypr/hyprlock.conf" 
[ -n "cfk" ] && export cfk="/home/tinker/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="/home/tinker/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="/home/tinker/.config/fish/config.fish" 
[ -n "cff" ] && export cff="/home/tinker/.config/fish/config.fish" 
[ -n "cfm" ] && export cfm="/home/tinker/.config/matugen/config.toml" 
