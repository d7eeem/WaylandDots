[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/shell/aliasrc" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "cff" ] && export cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="$HOME/Music" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/hyprdots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.local/src" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfz" ] && export cfz="$HOME/.config/zsh/.zshrc" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfaz" ] && export cfaz="$HOME/.config/shell/aliasrc" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfl" ] && export cfl="$HOME/.config/lf/lfrc" 
[ -n "cfL" ] && export cfL="$HOME/.config/lf/scope" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfp" ] && export cfp="$HOME/.zprofile" 
[ -n "cfe" ] && export cfe="$HOME/.zshenv" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="$HOME/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="$HOME/.config/fish/config.fish" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
[ -n "cac" ] && export cac="$HOME/.cache" 
[ -n "cf" ] && export cf="$HOME/.config" 
[ -n "Cff" ] && export Cff="$HOME/.config/fish" 
[ -n "cy" ] && export cy="$HOME/.config/yazi" 
[ -n "D" ] && export D="$HOME/Downloads" 
[ -n "d" ] && export d="$HOME/Documents" 
[ -n "dt" ] && export dt="$HOME/.local/share" 
[ -n "rr" ] && export rr="$HOME/.config/rofi/" 
[ -n "h" ] && export h="$HOME/" 
[ -n "m" ] && export m="/mnt/" 
[ -n "pp" ] && export pp="$HOME/Pictures" 
[ -n "sc" ] && export sc="$HOME/.local/bin" 
[ -n "ss" ] && export ss="$HOME/.local/bin/statusbar" 
[ -n "sd" ] && export sd="$HOME/.local/bin/dmenu" 
[ -n "sp" ] && export sp="$HOME/.local/bin/personal" 
[ -n "hs" ] && export hs="$HOME/.local/bin/hyde" 
[ -n "src" ] && export src="$HOME/.local/src" 
[ -n "vn" ] && export vn="$HOME/.config/nvim" 
[ -n "vnc" ] && export vnc="$HOME/.config/nvim/lua/core" 
[ -n "vnp" ] && export vnp="$HOME/.config/nvim/lua/plugins" 
[ -n "do" ] && export do="$HOME/WaylandDots/" 
[ -n "hh" ] && export hh="$HOME/.config/hypr/" 
[ -n "wb" ] && export wb="$HOME/.config/waybar/" 
[ -n "nc" ] && export nc="$HOME/Nextcloud/" 
[ -n "bf" ] && export bf="$HOME/.config/shell/bm-files" 
[ -n "bd" ] && export bd="$HOME/.config/shell/bm-dirs" 
[ -n "cfv" ] && export cfv="$HOME/.config/nvim/init.lua" 
[ -n "cfh" ] && export cfh="$HOME/.config/hypr/hyprland.conf" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfn" ] && export cfn="$HOME/.config/fontconfig/fonts.conf" 
[ -n "cfa" ] && export cfa="$HOME/.config/fish/aliasesrc.fish" 
[ -n "cft" ] && export cft="$HOME/.config/tmux/tmux.conf" 
[ -n "cfw" ] && export cfw="$HOME/.config/waybar/config.ctl" 
[ -n "cfk" ] && export cfk="$HOME/.config/kitty/kitty.conf" 
[ -n "cfg" ] && export cfg="$HOME/.config/ghostty/config" 
[ -n "cfp" ] && export cfp="$HOME/.config/fish/config.fish" 
[ -n "cff" ] && export cff="$HOME/.config/fish/config.fish" 
